import Vue from 'vue';

export default function (to, from, next) {
    if (!Vue.prototype.$store.getters['Authorization/isLoggedIn']) {
        next({name: 'login'});
        return;
    }
    next();
}
